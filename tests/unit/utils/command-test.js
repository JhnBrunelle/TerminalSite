import command from 'personalsite/utils/command';
import { module, test } from 'qunit';

module('Unit | Utility | command');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = command();
  assert.ok(result);
});
